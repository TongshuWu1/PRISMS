# Scripts

Runnable entry points and support scripts are grouped by role.

- `launchers/`: user-facing commands for controller experiments.
- `generation/`: CoppeliaSim plant and scene generation helpers.
- `analysis/`: offline telemetry/data analysis helpers.

Run these from the project root so relative paths resolve consistently.
