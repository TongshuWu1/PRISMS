# Archive

Old experiments that are no longer part of the active workflow.

- `obsolete_pair_configurations/`: pre-generated two-drone configuration models
  and tests. These were retired after switching to reusable single-drone modules
  that dock dynamically through the magnetic controller.
