# Scenes

Generated CoppeliaSim scenes for testing.

- `body_rate_controller_demo_scene.ttt`: single-drone controller demo scene.
- `two_drone_spawn_scene.ttt`: two-drone spawned scene.

Scenes are convenient test harnesses. The reusable drone itself belongs in
`model/`.
